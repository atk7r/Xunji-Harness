[project]
name = "vulnfinder"
version = "0.1.0"
description = "Claude Code autonomous red-team web initial-access workspace with deterministic safety boundaries."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = []

[tool.ruff]
line-length = 100
target-version = "py310"
