# Xunji runtime mode example
# Copy to config.ini for local overrides. Real config.ini is ignored by git.

[mode]
# normal = full checks: drift advisory + evidence gates + closure checks
# dev    = development mode: drift advisory records but does not block
# ghost  = autonomous hosted mode: relaxed drift stale threshold
mode = normal
