# Xunji runtime mode example
# Copy to config.ini for local overrides. Real config.ini is ignored by git.

[mode]
# normal = full enforcement: drift block + evidence gates + auto-closure
# dev    = development mode: drift detection records but does not block
mode = normal
