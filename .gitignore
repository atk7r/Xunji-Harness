# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local environments
.venv/
.env
.env.*

# Runtime output
runs/*
!runs/.gitkeep
artifacts/
observations/

# Manual/security-sensitive work products — never publish real target findings
reports/
poc/

# Weaponized 0day exploits ship with the repo (poc_library/xday/) — method is
# free. Scrub real-target instances first (findings hygiene, not the weapon).
# tools/poc_ours_upload/ stays out: redundant older build of an xday weapon.
tools/poc_ours_upload/

# Local scratch
tmp/
*.tmp

# active-tool runtime state
tools/harness/.state/
