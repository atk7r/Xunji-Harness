# Peer Review — 2026-07-08-setup-statusline-active-run-review

_backend: claude:code-cli · 2026-07-08T11:53Z_
> 候选, 非裁决。driver 须逐条过证据门: 不盲从(驳工具/语境误报), 不忽视(采纳真盲补)。

## Verdict: WARN

_backend: claude:code-cli_
_brain: codex_
_bundle_hash: 1919204b2176bec6c74a8d0a09cbb7d934b94689_
_evidence_index_hash: 1e8825cb9a1ff6833ec78a63df19bad564ef8a38_

## Findings
- [WARN] PR-001 E-005 prose cites stale artifact name `evidence/final-diff.txt` (line 74 of evidence.md) while the current authoritative artifact is `evidence/final2-diff.txt`. The artifacts field lists the correct `final2-*` files, but the narrative body was never updated after Round 3. | Evidence: `evidence.md:74` ("The authoritative current diff is now `evidence/final-diff.txt`") vs `evidence.md:79` (`Artifacts: evidence/final2-diff.txt`) | Why: Creates a contradiction within the evidence entry — a reader following the prose would look for the wrong file. The evidence.json correctly marks E-005 as `"superseded": true` (line 204) but evidence.md itself doesn't carry this marking.
- [WARN] PR-002 E-005 is marked `"superseded": true` in evidence.json (line 204) but remains in the `confirmed` evidence list at certainty 0.8 (line 168). A superseded evidence entry cannot simultaneously be confirmed — if it describes Round-2 fixes that are now themselves stale, its certainty should be downgraded or the entry marked as historical. | Evidence: `evidence.json:168-205` vs `evidence.json:11` (E-005 in confirmed list) | Why: Internal ledger inconsistency — the confirmed/superseded dual status is semantically contradictory.
- [WARN] PR-003 `review-panel-final2.md` returned `NEEDS_DRIVER` verdict with arkcli panel completely failed (all 3 models had parse errors), and this fourth panel run was never dispositioned in `review.md`. The review.md only covers three rounds (Round 1-3), but the evidence directory contains four panel outputs including the un-dispositioned `review-panel-final2.md`. | Evidence: `review-panel-final2.md:6` ("Verdict: NEEDS_DRIVER"), `review-panel-final2.md:13` (all arkcli models failed), `review.md:41-56` (Round 3 — no mention of fourth panel) | Why: The review architecture requires heterogeneous review (arkcli panel + Claude Code). When arkcli completely failed, the fallback verdict was `NEEDS_DRIVER` meaning completion gate was not met — but this was silently skipped in the final synthesis.
- [WARN] PR-004 `import xunji_statusline` is unconditional at module level (diff line 94: `+import xunji_statusline  # noqa: E402`). If `xunji_statusline.py` gains a broken transitive import (e.g., `status_style` imports a missing dependency), `setup_run.py` becomes entirely unreachable — even for operations that never touch the statusline. The `_set_active_run()` helper is documented as "best-effort," but the import coupling is not. | Evidence: `evidence/final2-diff.txt:94`, `evidence/final2-diff.txt:102-110` (_set_active_run best-effort docstring) | Why: Asymmetrical failure boundary — the function tolerates runtime failures but the module-level import creates a hard dependency that would break all setup_run functionality. A lazy import inside `_set_active_run()` would be more consistent with the best-effort design.
- [WARN] PR-005 `check_rules.py` output remains a single opaque line "rule check passed" across all four review rounds with no transparency into what rules were evaluated. | Evidence: `evidence/final2-test-log.txt:47` ("rule check passed"), flagged in `review-panel.md:34` (PR-006), `review-panel-rerun.md` (noted), `review-panel-final.md` (blind-spot), `review-panel-final2.md:35` (blind-spot) | Why: All four independent review rounds flagged this opacity gap, but no resolution or explanation was recorded. Without knowing what `check_rules.py` substantively evaluates, the safety gate provides no audit value.
- [WARN] PR-006 The `_set_active_run()` call site in `main()` is placed unconditionally after `scaffold()` with no comment documenting the assumption that every code path through `main()` should set the active-run pointer. A future mode added to `main()` could unknowingly inherit this behavior. | Evidence: `evidence/final2-diff.txt:120` (`+    _set_active_run(run_dir)`), `review-panel-rerun.md:42` (general principle under-documented) | Why: While D-005 correctly resolves the current `--classify` semantics, the assumption that ALL future code paths should set the pointer is an undocumented architectural constraint at the call site.

## Blind-spot check
- **Evidence discipline category mismatch**: All 6 evidence entries apply the web-vuln pentesting certainty framework (certainty 0.8, control-experiment rule) to a pure maintenance diff review. The "Control" fields (e.g., E-001: "The helper is best-effort and uses `xunji_statusline.set_active_run()` validation") don't satisfy the pentesting control-experiment rule ("first demonstrate the mechanism activates under normal conditions, then show its absence"). The evidence format shows genuine semantic strain when applied outside its original domain. None of the four review rounds addressed whether certainty 0.8 is meaningful for "does the diff add the code it claims to add." This is a process-design concern, not a correctness error, but it degrades the signal value of the evidence ledger.
- **The `rd3` variable used in failure-branch selftest is not defined in the diff**: `evidence/final2-diff.txt:159,169` uses `rd3` in `_set_active_run(rd3)` without defining it in the diff context. The test log confirms it resolves (line 1 shows it's a temp dir under `/var/folders/.../t3_20260101`), so this works correctly. But a reviewer reading only the diff cannot verify this — it's a diff-readability blind spot, not a defect.
- **`shutil`/`tempfile` imports not shown in diff**: The new selftest code uses `shutil.rmtree()` and `tempfile.mkdtemp()`, but the diff doesn't show their import statements. Since `setup_run.py` already uses `shutil.copy` for template scaffolding in earlier parts of `_selftest()`, they're certainly already imported. The Round 2 review flagged this as "Critical Issue 6: Missing `import shutil`" but it was a false alarm — the imports pre-exist the diff.
- **The three-round iterative fix history is well-executed**: Round 0 had real pointer mutation and temp dir leaks. Round 1 fixed both with monkeypatching and `shutil.rmtree`. Round 2 added module-pointer restoration assertions, real-pointer non-mutation check, and `--classify` doc clarity. Round 3 added failure-branch tests. Each round addressed substantive concerns and the evolution is well-documented. This is a positive observation — the review process worked despite the residual items above.
- **The selftest's `finally` block restores the module constant, then independently re-reads the original file to double-check**: `evidence/final2-diff.txt:142-152` — the "restores module pointer" check verifies the Python constant assignment, and the "leaves real pointer untouched" check independently re-reads the filesystem file. This dual-verification provides defense-in-depth against the restoration failing silently. Noted as a strength of the test design.

## Context-limit notes
- I was denied read access to `tools/setup_run.py` (full source), `tools/xunji_statusline.py`, `tools/check_rules.py`, and `.claude/skills/*` (full source). My assessment of import-time behavior, `set_active_run()` internals, `clear_active_run()` behavior, `shutil`/`tempfile` import existence, and `--classify` code paths relies on diff fragments, test output, and prior review claims. The test log passing provides strong circumstantial evidence but I cannot independently verify the non-diff portions.
- The Chinese-language skill files were only seen through diff hunks. The diff adds English text alongside existing Chinese prose. I cannot verify whether the surrounding Chinese prose creates any contradiction with the new additions. My assessment of documentation consistency (F-004) is therefore partial.
- `check_rules.py` contents are not in this review scope — my criticism of its opacity is a transparency concern, not an assertion that it contains defects. It might contain substantive checks that simply produce concise output.
- The evidence framework (certainty scale, control-experiment rule, evidence gate) was designed for web-vuln pentesting with target-side artifacts. My observation about category mismatch is a process-design observation — applying a different evidence framework to maintenance reviews might produce more meaningful certainty assignments, but the framework as-is doesn't produce false results, just lower-signal ones.