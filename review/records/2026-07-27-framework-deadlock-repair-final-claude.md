# Peer Review — review-final

_backend: claude:code-cli · 2026-07-27T09:17Z_
> 候选, 非裁决。driver 须逐条过证据门: 不盲从(驳工具/语境误报), 不忽视(采纳真盲补)。

## Verdict: BLOCKER

_backend: claude:code-cli_
_brain: codex_
_bundle_hash: c7f589d888cb918c1bff1f6ae4738214d5ac4208_
_evidence_index_hash: 7ba22831530dc69ec947309e0d6ea279e4a014b7_

## Findings
- [BLOCKER] PR-001 Missing `workers.py` diffs despite being the execution anchor for the entire stale-reviewer-replay architecture | Evidence: E-001/E-003 mention `workers.py delegate --limit 1` as the owner command, E-004 claims "reviewer recovery fixture now rejects tamper before returning a launch contract", but **zero diffs for `workers.py` exist in the evidence** | Why: The `stale_recovery_action()` classifier in `agent_settlement.py` is tested, the `turn_contract.py` gate is tested, and the `_check_agent_board` integration is tested with a **mock** in `run_gate.py` — but the actual execution code in `workers.py` that performs durable-row revalidation, bundle/artifact/dependency checking, and contract return is absent from every diff. Without it, the entire "idempotent replay" claim is unverifiable. E-004's prose assertion that "Reviewer recovery fixture now rejects both instruction-context tamper and dependency result-digest tamper before returning a launch contract" refers to code not shown in any evidence artifact.
- [BLOCKER] PR-002 E-004 verification artifact is prose, not raw test output | Evidence: `verification.md` is a 1163-byte narrative summary | Why: For certainty ≥ 0.8, the rubric requires artifact-backed confirmation. E-004's "69 passed, 0 failed in 117.2s / 112.4s" are written claims in a single text file, not raw test-runner logs, terminal captures, or CI pipeline artifacts. The sha1 proves file integrity, not claim veracity. This is the definition of "single observation / environment-provided artifact" which the rubric says is **never confirmation on its own**. This evidence should be downgraded below 0.8 (or the raw test runner output should be attached).
- [WARN] PR-003 Uniform certainty 0.8 across all evidence types masks evidence-quality differences | Evidence: entries E-001 through E-004 all have `certainties: [0.8]` | Why: A structural code change with compilable diff (E-001, E-002) is qualitatively different evidence from a prose verification file (E-004). The uniform grading suggests systematic mechanical application rather than evidence-based scrutiny. E-004 should be 0.5 at most without raw test artifacts.
- [WARN] PR-004 No backward-compatibility layer for legacy replay v1→v2 transition | Evidence: `probe.py` diff adds `"schema": "...probe.replay.v2"`, `wire_len`, `wire_sha1`, `saved_body_meta` | Why: The new schema fields are mandatory in the v2 shape. The diffs do not show any compatibility shim in downstream consumers (`workers.py`, disposition schema, `peer_review.py`) for legacy v1 replay files that lack these fields. E-003 docs claim "legacy replay files' len/sha1 was the complete raw response" but that's an assertion, not a code-enforced migration path. A v1 replay consumed by updated validation code may fail closed in ways not tested.
- [WARN] PR-005 Report framing contradicts evidence content | Evidence: the user's rubric treats this as "web-pentest run"; the evidence contains zero URLs, zero probe_*.html, zero scan output, zero vulnerability classifications | Why: The entire evidence set is a framework code review, not a penetration test. This framing mismatch means the coverage ledger (rubric item 2) cannot be assessed as a pentest — there are no assets, no classified fronts, no scope boundaries. Either the framing is wrong or the evidence is incomplete. This should be called out before any substantive finding is accepted.
- [WARN] PR-006 `run_gate.py` selftest for stale recovery uses a mock, not the real classifier | Evidence: `01-core-authority.diff` lines at `@@ -1665,2 +1700,29 @@`, where `_agent_settlement.stale_recovery_action` is replaced with a lambda returning a hardcoded response | Why: While `turn_contract.py` tests the real classifier via `evaluate_pretool`, the `_check_agent_board` integration test in `run_gate.py` never exercises the real decision tree. This means a logic error in `stale_recovery_action` that causes wrong classification for edge cases (e.g., reviewer `runtime_state="no-attempt"` with `reviewer_ready=False` and `assignment` populated → correctly enters `invalid_reviewers`, but `reviewer_ready=False` with `runtime_state="returned"` → **silently dropped from all lists**) would pass the `run_gate.py` selftest and only be caught by the `turn_contract.py` test if the exact scenario is covered.
- [WARN] PR-007 `for_emit` / risk of hidden key sort order changing behavior | Evidence: `tools/agent_settlement.py` at `@@ -71,2 +71,8 @@` defines `RECOVERY_*` constants, and at `@@ -673 +679,108 @@` the `stale_recovery_action` uses `key=lambda item: (str(item.get("lane_id") or ""), str(item.get("assignment") or ""))` for sorted() | Why: The function returns the **first matching action class** (replay > create > wait > cancel > invalid > replan). If a plan has BOTH a replay reviewer AND a cancellable non-reviewer lane, only the reviewer is returned. The caller gets no visibility that a secondary debt also exists. Multiple iterations of `stale_recovery_action` may be needed to drain all debt, but there's no evidence in the diffs that the callers in `turn_contract.py` or `run_gate.py` handle this correctly (they consume the first item without checking for remaining items).
- [WARN] PR-008 `_ended_plan_foreground_review_allowed` has a recoverable exception path that silently fails open | Evidence: `tools/turn_contract.py` at `@@ -4567,6 +4565,56 @@` | Why: The function has two `except Exception: return False` blocks (for `work_plan.transaction_bound_plan()` and `runtime_receipts.agent_attempts()`). If `transaction_bound_plan()` raises, it returns False (blocked — safe). If `agent_attempts()` raises, same. But if `_registered_capability_invocation()` inside the elif-instrumentation raises an unexpected exception, it's uncaught and propagates. More critically: the `work_plan.plan_cycle_ended(run_dir, plan)` call is after `transaction_bound_plan`, and if `plan_cycle_ended` raises (e.g., file-not-found), the exception is uncaught because only `transaction_bound_plan`/`agent_attempts` are wrapped. This is a narrow exception-safety gap.

## Blind-spot check
- **Pentest framing is a category error** — The evidence describes code changes to an agent lifecycle framework (assignment settlement, worker delegation, replay integrity, coda convergence). Reviewing it as a "web pentest" misses the relevant code-review criteria (test coverage, exception safety, backward compat, TOCTOU in stale classification → workers execution). The original reviewer likely accepted the pentest framing without questioning whether the evidence matches it.
- **No integration-level end-to-end test is shown** — Individual components are unit-tested (agent_settlement classifier, turn_contract gate, probe replay schema, peer_review CAS check), but there is no E2E test that creates a stale plan with a real Reviewer assignment, runs `workers.py delegate --limit 1`, verifies the durable replay return, runs review, issues `cycle_end`, and asserts final coda state. The verification only proves the components compile and pass isolated selftests.
- **`stale_settlement_reviewer_ready()` implementation is not in the diff** — E-001's `stale_recovery_action` depends on this function (called at line ~752 of the new code in `agent_settlement.py`), but the diff header `@@ -673 +679,108 @@ def stale_settlement_reviewer_ready(` means only the function **signature** and subsequent new code are shown — the body of `stale_settlement_reviewer_ready` itself is not included as evidence. A reviewer cannot verify what conditions constitute "reviewer ready" from the evidence alone.
- **No static analysis or type-checking evidence** — The bundle has `machine_findings: []` and no linter/mypy/security-scan output. For security-critical gates (execution authority, evidence CAS, replay validation), the absence of automated analysis is a coverage gap the author likely didn't flag.
- **No TOCTOU analysis between classification and execution** — `stale_recovery_action()` reads plan/projection state and returns a classification. The workers execution that acts on this classification happens later (possibly in a different turn), during which lane runtime states could change (e.g., a "running" lane that wasn't the first priority returns while the turn was being processed). The original review doesn't discuss this temporal gap.

## Context-limit notes
- I am operating entirely on the frozen review bundle JSON and diff excerpts. I cannot open the actual source files (`tools/agent_settlement.py`, `tools/workers.py`, etc.) to verify that the diffs apply cleanly or that surrounding code context supports the changes. The sha1 hashes on diff files confirm integrity, not correctness.
- The Chinese-language content in `docs/ARCHITECTURE.md`, `docs/WORKFLOW.md`, and `CLAUDE.md` is technical and appears well-formed, but I may miss subtleties in Chinese idiom that a native-speaker reviewer would catch. No findings depend on Chinese-text interpretation.
- The tag `<redacted:internal:...>` in multiple places suggests organizational-internal identifiers have been scrubbed. This is appropriate for external review but means I cannot verify cross-references against internal documentation or issue trackers.
- The "red-team web-pentest" framing versus the actual code-review content is the largest context concern. If this bundle was intended to demonstrate pentest evidence, it's missing all required artifacts. If it's a code-review bundle mislabeled, my pentest-rubric application may produce findings that feel genre-inappropriate. I've reviewed against both.
- There is a reference in the E-003 docs diff to a "Codex-author matrix" review where "arkcli's kimi timed out and glm output parsing failed, so the total verdict=NEEDS_DRIVER, not counted." This suggests an earlier independent review was incomplete due to tooling failures — this failure mode is not evaluated in the evidence but is an acknowledged gap in the review chain.