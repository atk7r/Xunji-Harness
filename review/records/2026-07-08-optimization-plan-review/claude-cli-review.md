# Peer Review — 2026-07-08-optimization-plan-review

_backend: claude:code-cli · 2026-07-07T21:24Z_
> 候选, 非裁决。driver 须逐条过证据门: 不盲从(驳工具/语境误报), 不忽视(采纳真盲补)。

## Verdict: WARN

_backend: claude:code-cli_  
_brain: codex_  
_bundle_hash: 8f44b7bd146b39def60facb17e88a2513d21ca5d_  
_evidence_index_hash: cba66f20669004a6330988747f4b721dfb6cd03a_  

## Findings
- [WARN] PR-001 threat-hypothesis-closure canary measures template compliance, not discovery improvement | Evidence: optimized-plan.md:62-66, tools/bench.py:3-34 | Why: The acceptance criterion 'fails before and passes after' can be satisfied by agents mechanically filling threat hypothesis fields without the hypothesis leading to a confirmed finding or falsification. The canary name implies closure improvement, but the measured signal is field presence, not outcome quality. Without a link to evidence entries or certainty upgrades, the canary is a paperwork gate.
- [WARN] PR-002 Six new canaries added while ROADMAP confirms bench fixtures are sparse — optimizing for advanced patterns before basic coverage | Evidence: docs/ROADMAP.md:191-195, optimized-plan.md:32-37 | Why: ROADMAP states 'bench v0 landed but fixtures are still sparse.' Adding `signed-client-param`, `state-machine-skip`, `replay-or-race-logic` before fixtures exist for SQLi, XSS, IDOR, path traversal, or auth bypass means the bench cannot answer the most basic question: does Xunji detect common vuln classes at all? The canary list assumes the framework already handles fundamentals and needs refinement — but that assumption is unverified.
- [WARN] PR-003 Skill placement boundary unaddressed: new Agent prompt changes and tools don't specify `.claude/skills/` vs `.agents/skills/` landing | Evidence: report.md:15-17, optimized-plan.md:49-60 | Why: The repo constraints state Claude Code is primary driver and ambiguous changes default to `.claude/skills/`. Phase 1 adds Agent `## New Threat Hypotheses` output and Phase 4 adds mentor hints — both are Agent prompt/behavior changes that could land in either skill tree. If they land in `.agents/skills/`, they become Codex-auxiliary artifacts that the live driver may not load or enforce. The optimized plan is silent on placement.
- [WARN] PR-004 No verification that existing loop_state.py signals, surface.md discovery channels, or frontier.md unruled-out fields are actually effective before layering new mechanisms | Evidence: tools/loop_state.py:227-254, docs/templates/run/surface.md:23-49, docs/templates/run/frontier.md:36-39, optimized-plan.md:47-60 | Why: The optimized plan extends existing templates with threat hypothesis fields (Phase 1) and adds JS discovery fields (Phase 2), assuming the current discovery channels and loop-state signals work. But if agents already ignore `surface.md` JS refs or `loop_state.py` no-progress signals in practice, adding more fields to the same templates won't change agent behavior. The plan adds mechanism without first measuring whether existing mechanism is consumed.
- [WARN] PR-005 Arkcli panel review was effectively single-model (minimax-m3 and glm-5.2 both errored); Claude Code fresh-context leg returned empty — the review foundation is weakened | Evidence: arkcli-review.md:21-24, arkcli-review.md:33, arkcli-review.md:45-46, arkcli-review.md:57, claude-review.md:13-20 | Why: The review architecture specifies a multi-model arkcli panel (kimi-k2.7-code + minimax-m3 + glm-5.2). Two of three backends had parse errors, reducing the panel to single-model. The Claude Code fresh-context leg produced zero findings, which is anomalous — a fresh reader of the plan should have surfaced at minimum the threat_model/surface redundancy. The driver's optimized plan was synthesized from incomplete review input and should not be treated as having passed a full multilateral gate.
- [WARN] PR-006 events.jsonl v2 deferral is correct, but the plan doesn't verify whether v1 consumers exist — building v2 on unread v1 is wasted effort | Evidence: tools/state_project.py:97-123, optimized-plan.md:127-145 | Why: The optimized plan correctly defers events.jsonl v2 and keeps v1 derived from Markdown. But if no downstream consumer (dashboard, bench scorer, review hook) actually reads state_project.py v1 events, then even the deferred v2 has zero value regardless of schema. The plan should first confirm v1 is consumed before planning any v2 work.

## Blind-spot check
- Neither the original plan nor the arkcli panel asked whether agents actually consume the existing surface.md JS discovery channels, loop_state.py signals, or frontier.md unruled-out fields. The plan assumes these mechanisms work and need extension — but if agents ignore them today, adding more fields changes nothing. A 'template field consumption' bench canary should precede all Phase 1-2 work.
- The plan borrows from 6 external projects but doesn't assess whether those projects' success conditions match Xunji's. Pentagi is an interactive mentor for human pentesters; Pentest-Lyan is a static threat-modeling methodology. Their mechanisms may not transfer to an autonomous agent loop. A 'borrowed-mechanism applicability' filter is missing.
- No-recon mode means coverage.json and classify/ don't exist. The optimized plan's threat hypothesis generation (Phase 1) and conditional permission matrix (Phase 3) don't explicitly handle the no-recon case — they may assume surface enumeration data that isn't present.
- The plan defers Graphiti/pgvector memory but doesn't address the current episodic context limitation: agents in later cycles don't see earlier cycles' full reasoning. Threat hypotheses generated in cycle 3 can't reference why a vector was deferred in cycle 1. The optimized plan's 'linked IS/C/E' field assumes cross-cycle linkage that may not survive context windows.

## Context-limit notes
- Read-only mode prevented direct inspection of tools/bench.py, tools/state_project.py, tools/loop_state.py, docs/templates/run/*.md, docs/ROADMAP.md, and docs/WORKFLOW-reference.md. All findings rely on line-range claims in the review bundle evidence anchors, which I cannot verify against live files.
- No access to current bench fixtures — cannot confirm whether any basic-vuln canaries exist or whether the claimed sparsity is accurate.
- No access to live run data — cannot verify whether agents actually consume existing surface.md/frontier.md/loop_state.py outputs in practice.
- The arkcli panel's backend errors (minimax-m3, glm-5.2) and Claude Code fresh-context empty output mean I'm effectively the second working reviewer after kimi-k2.7-code, not the Nth reviewer in a full panel. True multilateral coverage is 1.5/3 for arkcli and 1/2 for Claude legs.
- The optimized-plan.md is a synthesis artifact — I cannot verify whether the driver faithfully incorporated arkcli feedback or selectively adopted convenient findings.