{
  "schema": "xunji.peer_review_result.v1",
  "verdict": "WARN",
  "backend_used": "panel:arkcli+claude",
  "driver": "codex",
  "brain": "codex",
  "bundle_hash": "dab998323fd67f0bd44f3a4b96a1df6a0be56606",
  "evidence_index_hash": "5f0b443ae9998608cf8e6d10197919a0d476fa61",
  "findings": [
    {
      "id": "PR-001",
      "severity": "WARN",
      "category": "claim_integrity",
      "claim": "E-003 confirms the Claude Code statusLine contract as stable and sufficient to justify removing all cwd/PWD/os.getcwd() fallbacks.",
      "evidence": "claude_statusline_contract.txt:1-15, statusline.diff:69-86 (hunk @@ -89,16 +69,18 @@ def _workspace_dir)",
      "affected_eids": [
        "E-003"
      ],
      "recommended_action": "Downgrade E-003 certainty to <=0.5 and either retain a defensive fallback for a missing workspace payload or document that the statusline is only guaranteed for Claude Code v2.1.201+ and requires a synthetic payload for manual invocation.",
      "why": "[panel:arkcli] [arkcli:kimi-k2.7-code] The contract evidence is a single reverse-engineered observation of one installed binary version (v2.1.201). It is an environment-provided artifact, not a published API guarantee. Removing every fallback means a payload shape change or non-Claude invocation silently yields an empty statusline even inside a valid Xunji workspace.",
      "status": "pending"
    },
    {
      "id": "PR-002",
      "severity": "WARN",
      "category": "claim_integrity",
      "claim": "E-002 regression verification proves the selftest does not mutate the operator's persistent active-run pointer.",
      "evidence": "statusline.diff:319-320 (global ACTIVE_RUN reassignment), statusline.diff:328-342 (watched file list excludes real pointer and turn_contract.json), statusline.diff:535+ (checks list only compares ACTIVE_RUN variable)",
      "affected_eids": [
        "E-002"
      ],
      "recommended_action": "Add a before/after stat or checksum assertion on the real .claude/xunji_active_run file and on run/state/turn_contract.json for the source run during active-run switch inheritance; verify that no real pointer mutation occurs even if active_run/set_active_run reference a separately imported constant.",
      "why": "[panel:arkcli] [arkcli:kimi-k2.7-code] The test only checks that the module-level ACTIVE_RUN variable is restored; it does not verify the real filesystem pointer file is untouched. set_active_run's turn-contract inheritance writes to source run state (turn_contract.json), which is also excluded from the read-only mtime assertions, so the 'no mutation' claim is weaker than stated.",
      "status": "pending"
    },
    {
      "id": "PR-003",
      "severity": "WARN",
      "category": "missed_surface",
      "claim": "The simplified statusline selftest and docs cover the CLI/stdin invocation path after the subprocess color path was removed.",
      "evidence": "statusline.diff:491-510 (subprocess.run XUNJI_COLOR test removed), docs/AI_ENV_SETUP.md:149-163 (manual hook simulation text), selftest.log:1-8",
      "affected_eids": [
        "E-002"
      ],
      "recommended_action": "Add a minimal stdin JSON test for the CLI entry point, and update docs/AI_ENV_SETUP.md to show that manual invocation must pipe a synthetic workspace payload because PWD/cwd fallbacks were removed.",
      "why": "[panel:arkcli] [arkcli:kimi-k2.7-code] After removing the subprocess env test and the cwd/PWD fallbacks, the only remaining verification is direct function calls. Operators following the existing '手动模拟 hook' instructions may run the script from the project root and incorrectly conclude the statusline is broken when it prints nothing.",
      "status": "pending"
    },
    {
      "id": "PR-004",
      "severity": "WARN",
      "category": "backend_error",
      "claim": "arkcli panel had backend errors; review is partial",
      "evidence": "glm-5.2: parse error; output tail:  behavior. ✓\n\nIssue 11: The selftest assertion `\"missing cache still renders only phase and run\"` uses `missing_plain.startswith(\"[Xunji-status] [\")` and `missing_plain.endswith(f\"] {missing_cache.name}\")` and `\" | \" not in missing_plain`. This is a weaker assertion than the exact equality used for `plain`. The `missing_cache` run has `frontier.md` with content but no state files. The `loop_state.derive` would derive from markdown. The phase might be Hunter or something else. The startswith/ends",
      "affected_eids": [],
      "recommended_action": "rerun_or_escalate",
      "why": "[panel:arkcli] At least one arkcli reviewer failed, so PASS only means the completed panel members found no blocker.",
      "status": "pending"
    }
  ],
  "blind_spots": [
    "[arkcli] [kimi-k2.7-code] The selftest checks field leakage with populated legacy controller.shadow.json, assignments.json, and asset_ledger.json, but it does not exercise a stale loop_state.json with a newer frontier.md while legacy summary files are present; the stale-derivation path could still import modules that have side effects despite write=False.",
    "[arkcli] [kimi-k2.7-code] Nested workspace directories (e.g., ROOT/tools) are treated as Xunji context and will display the active run; this may be surprising if an operator opens a sub-directory for an unrelated task, but it appears intentional and is tested.",
    "[arkcli] [kimi-k2.7-code] No evidence evaluates the behavior when workspace.current_dir points to a run directory rather than the project root; _is_xunji_context likely still true, so the statusline will display the active run even if the user is inside a different run folder.",
    "[claude] **Prior rounds missed the subprocess removal gap completely.** Round 1 PR-003 noted `unknown_phase` appeared dead, but the bigger issue is that the entire stdin→stdout integration test (the production code path) was removed. The 3 consecutive rounds of review fixated on field leakage, workspace gating, and active-run pointer isolation — all valid concerns — but none flagged that the hook invocation path is now entirely untested. This is the highest-value finding I have.",
    "[claude] **The data flow from `_journal_summary()` is partially consumed but structurally unchanged.** The function returns an `interrupted` key that `render_statusline()` no longer reads as a separate suffix. It IS still consumed by `_phase()` internally (which reads `journal[\"interrupted\"]` to return `\"Interrupted\"`). This is correct design but creates a subtle coupling: if someone removes the `interrupted` computation from `_journal_summary()`, the phase tag also breaks. A cross-reference comment would help.",
    "[claude] **The `before_render == after_render` read-only assertion now over-covers.** The `watched` list checks mtimes of 6 files including `controller.shadow.json`, `assignments.json`, `asset_ledger.json` — files the new statusline never opens. The assertion passes trivially for these 3 files because nothing reads them. This is technically correct (they remain unchanged) but gives a false sense of test coverage for those specific file paths.",
    "[claude] **Positive negative-test design is excellent.** The selftest writes populated legacy files (`controller.shadow.json` with blockers, `asset_ledger.json` with summary, `assignments.json` with agents) then asserts `\" | \" not in plain`. This proves that even when legacy state files contain data, the simplified renderer does not leak fields. This is a robust regression guard and the right pattern.",
    "[claude] **The evidence chain is structurally sound for a maintenance review.** E-001 cites `statusline.diff` + `docs.diff` + `reviewed.diff` + `selftest.log` — the diffs are static proof of what changed; the log is environmental corroboration. E-002 adds the diff (containing exact assertion code) to the log. E-003 is a static payload contract inspection. For pentest evidence, certainty 1.0 from an environment log alone would be insufficient, but for a deterministic code change verified by static diff + passing test run, the combined artifacts meet the bar.",
    "[claude] **The CLAUDE.md update WAS applied correctly** (contrary to Round 1's blind-spot). The `docs.diff` shows `CLAUDE.md` as the second changed file, and my injected system-prompt context confirms the live version reads: \"Claude Code statusline is a read-only indicator for this project. It prints nothing without an explicit Xunji workspace and active run; otherwise it shows only `[Xunji-status] [<phase>] <run>`.\" Round 2 correctly corrected this."
  ],
  "context_limits": [
    "[arkcli] glm-5.2: parse error; output tail:  behavior. ✓\n\nIssue 11: The selftest assertion `\"missing cache still renders only phase and run\"` uses `missing_plain.startswith(\"[Xunji-status] [\")` and `missing_plain.endswith(f\"] {missing_cache.name}\")` and `\" | \" not in missing_plain`. This is a weaker assertion than the exact equality used for `plain`. The `missing_cache` run has `frontier.md` with content but no state files. The `loop_state.derive` would derive from markdown. The phase might be Hunter or something else. The startswith/ends",
    "[claude] I cannot directly read `tools/xunji_statusline.py` or `CLAUDE.md` from the live filesystem (Read/Grep both denied by hooks). I rely exclusively on the review bundle artifacts: `statusline.diff`, `docs.diff`, `reviewed.diff`, `selftest.log`, `claude_statusline_contract.txt`, `evidence.md`, `report.md`, `evidence.json`, and the 3 peer review rounds. The system-prompt-injected CLAUDE.md confirms the live update.",
    "[claude] I cannot verify that `_phase()`, `_journal_summary()`, `_read_input()`, or `status_style` module bodies are unchanged beyond what the diffs show. The diffs do not include changes to these functions, so I rely on that plus the `60/60` selftest pass.",
    "[claude] The `temp` variable in `ACTIVE_RUN = temp / \"xunji_active_run\"` is not visible in the diff hunks — it must be from an unchanged portion of `_selftest()`. The 60/60 pass confirms it resolves correctly at runtime.",
    "[claude] This is a repository maintenance change, not a pentest. The evidence discipline rubric (certainty ≥ 0.8, no single-observation confirmation) applies loosely. The combined static diff + execution log evidence chain is appropriate for a code change.",
    "[claude] Chinese documentation in WORKFLOW.md and AI_ENV_SETUP.md was read for consistency; I may have missed operator-facing nuance that a native speaker would catch."
  ],
  "error": ""
}