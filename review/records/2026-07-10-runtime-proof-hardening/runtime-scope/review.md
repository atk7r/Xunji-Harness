# Review

Round-one and round-two outputs are retained as `peer_review.round1.*` and
`peer_review.round2.*`; their adjudication is in `disposition.md`. The final
independent rerun will be written to `peer_review.md` and `peer_review.json`.
