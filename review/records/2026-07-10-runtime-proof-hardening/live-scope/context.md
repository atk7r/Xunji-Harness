# Context

All sessions use temporary run directories and sentinel-only local effects. Raw
stream JSON is retained in the parent maintenance record; each compact summary
contains its raw-file SHA-256 and the reviewed source script is bundled.
