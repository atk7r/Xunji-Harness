# Context

This scope verifies that written Claude Code guidance matches the deterministic
runtime controls. It is separated from closure code so every cited diff fits the
external panel context without truncation.
