# Peer Review Panel — receipts-scope

_backend: panel:arkcli+claude · 2026-07-10T22:04Z_
> 候选, 非裁决。driver 须逐条过证据门。

## Verdict: WARN

_backend: panel:arkcli+claude_  
_brain: codex_  
_bundle_hash: dce639231e4d8601face09277c817e90a6240043_  
_evidence_index_hash: f4796c5572e93c35de1ff255fd1be1083a625094_  

## Findings
- [WARN] PR-001 E-002's runtime_observation primary and replica traces are live installed-entrypoint hook observations with transcript-backed denials. | Evidence: runtime_observation.summary.json, runtime_observation.replica.summary.json, runtime_receipts.lines-255-264.txt | Why: [panel:arkcli] [arkcli:kimi-k2.7-code] Denied events in runtime_observation.summary.json have empty tool_use_id values. The framework's own _transcript_has() returns False for empty ids, which would exclude these events from denied_tool_events() and unresolved_target_denials(). Synthetic session ids (s-exec, s-explain, s-pause) and fixture tool_use_ids (agent-tool-1, cron-list-1) indicate the data is generated by a self-test, not by live installed hooks. The claim of two independent installed-entrypoint traces is therefore not fully supported.
- [WARN] PR-002 live_fanout_flow demonstrates the installed hooks enforcing controls in a realistic Claude Code session. | Evidence: live_fanout_flow.source.txt, live_fanout_flow.summary.json | Why: [panel:arkcli] [arkcli:kimi-k2.7-code] The test script writes a temporary settings.json in a temp directory and invokes `claude -p --dangerously-skip-permissions --tools Agent,Bash`. This bypasses the normal permission flow and does not prove the repository's installed .claude/settings.json enforces the claimed controls in a real operator session.
- [WARN] PR-003 E-001 confirms named adversarial controls through observed runtime work. | Evidence: adversarial_selftests.summary.json, runtime_receipts.lines-564-731.txt, workers.diff, settings.diff | Why: [panel:arkcli] [arkcli:kimi-k2.7-code] E-001's support is limited to selftests passing, source code excerpts, and diffs. The framework's own tools attest to their own correctness. No independent red-team run or external witness validates the forged-chain, bare-reviewer-PASS, or loose-Cron scenarios in a live installed environment.
- [WARN] PR-004 The full repository regression is recorded in the parent maintenance record and need not be presented here. | Evidence: report.md, decisions.md | Why: [panel:arkcli] [arkcli:kimi-k2.7-code] report.md states the full regression remains in the parent maintenance record, but no artifact, hash, or evidence_index entry cites that record. A reviewer cannot arbitrate this claim from the frozen bundle.
- [WARN] PR-005 arkcli panel had backend errors; review is partial | Evidence: minimax-m3: parse error; output tail: d.

Actually, looking at this more carefully:
- live_fanout_flow.summary.json shows 19 events
- runtime_observation.replica.summary.json shows 21 events
- runtime_observation.summary.json shows 21 events

So the live_fanout_flow is a separate test (19 events) and the runtime_observation is the replica (21 events). The replica is independent (different digest) but produces the same outcome.

The replica's event_digest_sha256 is 8aaf... which is different from the primary's raw_sha256 of 6cedf4...; glm-5.2: parse error; output tail: s, they would get an empty list for these events. This could be a problem.

However, looking at the `unresolved_target_denials` function - it calls `denied_tool_events()` which requires transcript backing. The summary shows `"unresolved_target_denials": []` which could mean either (a) all target denials were resolved, or (b) no target denials were transcript-backed so none were returned.

Actually, looking at the live_fanout_flow.summary.json: `"unresolved_target_denials": []` - this is from the | Why: [panel:arkcli] At least one arkcli reviewer failed, so PASS only means the completed panel members found no blocker.

## Blind-spot check
- [arkcli] [kimi-k2.7-code] Empty tool_use_id values in runtime_observation denied events likely went unnoticed as a violation of the framework's transcript-backing rule.
- [arkcli] [kimi-k2.7-code] Synthetic self-test events may have been treated as equivalent to live installed-entrypoint observations.
- [arkcli] [kimi-k2.7-code] The effect of --dangerously-skip-permissions on hook realism may not have been considered.
- [arkcli] [kimi-k2.7-code] No independent attestation exists for the live_fanout_flow result; all metrics are computed by the test script.
- [claude] **The sentinel-file check is post-hoc**: `live_fanout_flow.py` checks `sentinel.exists()` and `sentinel.read_text()` after Claude Code exits (lines 178-179). If the temp directory were not properly isolated (mitigated by `mkdtemp`), or if another process wrote the sentinel during the test, the check could false-pass. The dual check with `probe_allowed` (PreToolUse hook success + sentinel) provides redundancy but depends on the hook event being correctly captured in the stream-json output.
- [claude] **Completion review in live trace uses a different evidence hash**: The replica's `completion_review_valid` test uses `evidence_index_hash: f878ffbb...` while this scope's evidence index sha1 is `f4796c55...`. This means the completion-review gate was tested generically but not against this specific scope's evidence index. The generic test proves the mechanism, but a scope-specific completion review would be stronger evidence for F-001 closure.
- [claude] **Event 20-21 in runtime observation share the same input_sha256**: Both seq 20 (bare PASS, rejected) and seq 21 (structured PASS, accepted) have `input_sha256: 4342c87f...` but different `response_sha256`. This indicates the same Agent prompt was issued twice and Claude produced two different responses — one that satisfied the gate and one that didn't. The test's correctness depends on non-deterministic LLM output, which is a fragility in the test design.
- [claude] **No `peer_review.py` live invocation**: The `review.md` correctly states peer review is still required, and the mechanism exists in code. But no evidence entry demonstrates a live `peer_review.py --into-run` invocation with captured markers. This is by design (deferred to the current review session), but it means the full review invocation validation chain (`review_invocation_valid` in runtime_receipts.py lines 459-493) is only selftest-covered, not live-flow-covered.

## Context-limit notes
- [arkcli] minimax-m3: parse error; output tail: d.

Actually, looking at this more carefully:
- live_fanout_flow.summary.json shows 19 events
- runtime_observation.replica.summary.json shows 21 events
- runtime_observation.summary.json shows 21 events

So the live_fanout_flow is a separate test (19 events) and the runtime_observation is the replica (21 events). The replica is independent (different digest) but produces the same outcome.

The replica's event_digest_sha256 is 8aaf... which is different from the primary's raw_sha256 of 6cedf4...
- [arkcli] glm-5.2: parse error; output tail: s, they would get an empty list for these events. This could be a problem.

However, looking at the `unresolved_target_denials` function - it calls `denied_tool_events()` which requires transcript backing. The summary shows `"unresolved_target_denials": []` which could mean either (a) all target denials were resolved, or (b) no target denials were transcript-backed so none were returned.

Actually, looking at the live_fanout_flow.summary.json: `"unresolved_target_denials": []` - this is from the
- [claude] The Chinese-language content in the evidence (frontier.md, decisions.md, report.md, review.md) is straightforward technical documentation. No Taiwan/CNVD-specific context applies. The denial reason messages in the `runtime_observation.summary.json` are in Chinese and I've interpreted them correctly based on the English CLI messages visible in `live_fanout_flow.source.txt`.
- [claude] The `workers.diff` and `anti_drift.py` changes reference Xunji-internal conventions (Agent Board, SharedBarrierGroup, `run_model.summary()`) that I've interpreted from the code context. If there are subtle Xunji-internal semantics I'm missing about how `_check_agent_board_needed` interacts with the receipt chain, my WARN on SubagentStart/SubagentStop might under- or over-weight.